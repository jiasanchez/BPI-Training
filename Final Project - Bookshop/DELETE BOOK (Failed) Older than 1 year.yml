info:
  name: DELETE BOOK (Failed) Older than 1 year
  type: http
  seq: 7

http:
  method: DELETE
  url: http://localhost:8080/api/bookshop/6
  auth:
    type: basic
    username: admin
    password: adminpw

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
