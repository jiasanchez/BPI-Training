info:
  name: READ BOOK
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/api/bookshop
  auth:
    type: basic
    username: admin
    password: adminpw

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
